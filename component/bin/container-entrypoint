#!/bin/bash -eu

source /opt/app-root/etc/mariadb_environment

envsubst < /opt/app-root/etc/mysqld/conf.d/tuning.cnf.template > /opt/app-root/etc/mysqld/conf.d/tuning.cnf

exec docker-entrypoint.sh mysqld
